<%= render :partial => 'order_header' %>
<% if !@order.approved %>
    <% if current_user.admin %>
        Order in name of: <%= @order.user.name %>&emsp;
        <span><%= link_to  "Approve this order", order_path, method: :patch, class: "text-white bg-success" %></span>  
    <% else %>
        <span><%= link_to  "Edit this order", edit_order_path(@order), class: "text-white bg-primary" %></span>
    <% end %>
    <span><%= link_to  "Delete this order", order_path, method: :delete, class: "text-white bg-danger" %></span>
<% end %>

<table class="table">
    <thead>
        <tr>
            <th scope="">Item</th>
            <th scope="col">quantity</th>
            <th scope="col">Item Price</th>
            <th scope="col">Total Price</th>
        </tr>
    </thead>
    <tbody>
        <% @order.order_items.each do |oi| %>
            <tr>
                <th scope="row"><%= oi.item.name %></th>
                <td><%= oi.quantity %></td>
                <td><%= number_to_currency(oi.item.price) %></td>
                <td><%= number_to_currency(oi.item.price * oi.quantity) %></td>
                <td></td>
            </tr>
        <% end %>
    </tbody>
</table>

